// sections
import { SteamLoginView } from 'src/sections/auth';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Steam: Login',
};

export default function LoginPage() {
  return <SteamLoginView />;
}
