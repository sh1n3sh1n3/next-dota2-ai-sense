// sections
import { AppView } from 'src/sections/app/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Home',
};

export default function HomePage() {
  return <AppView />;
}
